# 13 · 规模与架构：Scaling Law、数据与 MoE

> 中文 · [English](en/13-scaling-and-moe.md)

> 系列《从零看懂大模型》第 13 篇。前面讲了模型怎么算、怎么练、怎么服务、怎么用；这一篇回答一个被问得最多、也被答得最含糊的问题：**模型越大越聪明吗？**答案分三层：参数、数据、算力三者要按比例配（scaling law）；同样的算力，怎么分给"大"和"多"在变（Chinchilla 与它之后）；以及为什么现在的大模型可以"大而不贵"（MoE）。课本是 transformers 里 Mixtral 的 MoE 层源码。

## 一、Scaling law：三个量，一条幂律

![损失随参数、数据、算力按幂律下降](images/sc_powerlaw.png)

2020 年 Kaplan 等人在 OpenAI 做了一件朴素的事：把模型从几十万参数一路训到十亿级，把数据从几百万 token 一路加到几百亿，每种配置都记下最终的损失（第 4 篇的 loss，也就是"猜下一个词猜得多差"）。画出来是三条几乎笔直的线（对数坐标下）：

- 损失随**参数量 N** 按幂律下降；
- 损失随**数据量 D** 按幂律下降；
- 损失随**训练算力 C** 按幂律下降。

跨了七个数量级都是直线。这就是 scaling law：**在这三个量都不成为瓶颈的前提下，投入翻十倍，损失就稳定地降一小截**，可以预测，不靠运气。

两个直接推论决定了后来几年的走向。第一，**大模型是可以提前算出来的**：训一个小模型，量出这条线的斜率，就能预测一百倍算力下的损失。GPT-4 技术报告里那张"用千分之一算力预测最终 loss"的图，就是这么来的。第二，**三个量必须一起长**。参数堆上去、数据不够，线就弯了（过拟合）；数据够、参数太小，线也弯（欠拟合）。瓶颈总是最短的那块板。

一个常见误解要澄清：scaling law 描述的是**损失**，不是某个具体能力。损失平滑下降，某项任务的准确率却可能长期趴在零附近，然后在某个规模突然起来。这就是"涌现"。2023 年 Schaeffer 等人指出，很多涌现现象是**度量方式**造成的：用"完全答对才算分"的指标看是跳变，换成连续的指标看就是平滑上升。所以更稳妥的表述是：底层能力在平滑增长，只是有些任务的门槛是台阶状的。

## 二、Chinchilla：同样的钱，该买"大"还是买"多"

![同样算力：Gopher 280B 对 Chinchilla 70B](images/sc_chinchilla.png)

Kaplan 的原始结论里有一条：算力增加时，应该把大部分增量花在参数上，数据只需增加一点。整个行业照做了两年，模型一路做到 1750 亿、2800 亿参数，训练数据却停在三千亿 token 左右。

2022 年 DeepMind 的 Hoffmann 等人重做了这个实验，控制变量更严格（尤其是学习率调度要随训练长度调整，Kaplan 当年没做），得出相反的结论：**算力增加时，参数和数据应该等比例增长**。经验法则是每个参数配大约 20 个 token。按这条法则，当时的大模型全都"参数过大、数据不足"。

他们用同一份算力预算训了一个 700 亿参数、1.4 万亿 token 的模型（Chinchilla），对比自家 2800 亿参数、3000 亿 token 的 Gopher。**参数只有四分之一，数据多了四倍多，几乎所有基准全面超过。**而且推理时便宜四倍，因为第 5 篇讲过，推理成本主要看每个 token 要搬多少参数。

这是"越大越聪明"第一次被公开证伪：不是模型不该大，是**在固定预算下，大得过头就是浪费**。

## 三、Chinchilla 之后：为了推理便宜，故意"过度训练"

Chinchilla 算的是**训练**算力最优。但一个模型训一次，要服务几亿次。把推理成本算进总账，最优点就往"更小的模型、更多的数据"偏移：多花训练的钱，换每次推理便宜。

于是 2023 年起，行业集体走向"过度训练"（overtraining）：

| 模型 | 参数 | 训练 token | token / 参数 |
|---|---|---|---|
| Chinchilla（2022） | 70B | 1.4T | 20 |
| LLaMA 1（2023） | 7B | 1T | ~140 |
| Llama 3（2024） | 8B | 15T+ | ~1900 |

Llama 3 的 8B 模型按 Chinchilla 法则"只该"喂 1600 亿 token，实际喂了近百倍。损失仍在下降，只是越来越慢（对数线性）；但每次推理的成本按参数算，一分没涨。**这是"小模型现在也够用"的第一个原因：它们吃的数据是当年大模型的几十倍。**

数据成了新瓶颈。三个应对：

- **去重与过滤。**同一份网页语料，去重、按质量打分过滤后，同样 token 数训出来的模型明显更好。FineWeb 之类的开放数据集的主要贡献就是过滤流水线，不是抓了更多网页。
- **重复使用。**Muennighoff 等人（2023）测得，高质量数据重复训到约 4 遍，收益和新数据差不多；再多就迅速衰减。
- **合成数据。**用强模型生成、过滤后再训小模型。这与下一节的蒸馏是一回事的两面。

## 四、蒸馏：小模型的第二个来源

除了多喂数据，小模型变强的另一条路是**向大模型学**。

第 4 篇的预训练是拿真实文本当老师："下一个词是什么"。蒸馏是拿大模型当老师：让大模型对同一批输入给出输出（或给出完整的概率分布），小模型去拟合。大模型的输出比原始网页干净、一致、少噪声，所以同样参数量的小模型，蒸馏出来的通常比只吃原始数据的好一截。推理模型这一波尤其明显：DeepSeek-R1 公开的几个小尺寸版本，就是用 R1 生成的推理轨迹去微调 Qwen 和 Llama 得到的。

把这两节合起来：**今天一个 80 亿参数的模型，吃了百倍于 Chinchilla 法则的数据，其中一部分还是更强模型的输出。它和三年前一个 800 亿参数模型比，赢并不奇怪。**

## 五、MoE：大而不贵

![Dense 与 MoE：每个 token 只叫两个专家](images/sc_moe.png)

前面都在说"小"。但顶部的模型确实还在变大，只是变大的方式换了。

回忆第 2 篇的 Transformer block：attention 之后是一个前馈网络（FFN），两层线性变换，占了每层参数的大约三分之二。**Dense 模型**里，每个 token 都要过完整的 FFN。

**MoE（Mixture of Experts，混合专家）**把这一个 FFN 换成 E 个并排的 FFN（专家），再加一个很小的**路由器**。每个 token 进来，路由器打分，只把它送给得分最高的 k 个专家，其余专家对这个 token 不算。

以 Mixtral 8x7B 为例：8 个专家，每个 token 用 2 个。**总参数 467 亿，每个 token 实际参与计算的只有 129 亿。**训练和推理的计算量按 129 亿算，能力接近同期 700 亿的 dense 模型。DeepSeek-V3 把这个比例拉得更开：总参数 6710 亿，每个 token 激活 370 亿。

### transformers 里的实现

`modeling_mixtral.py` 的 `MixtralSparseMoeBlock` 就是这几步（略去张量整形）：

```python
router_logits = self.gate(hidden_states)                       # (tokens, n_experts)，一个线性层
routing_weights = F.softmax(router_logits, dim=1, dtype=torch.float)
routing_weights, selected_experts = torch.topk(routing_weights, self.top_k, dim=-1)
routing_weights /= routing_weights.sum(dim=-1, keepdim=True)   # 选中的 k 个重新归一

expert_mask = F.one_hot(selected_experts, num_classes=self.num_experts).permute(2, 1, 0)
for expert_idx in range(self.num_experts):
    idx, top_x = torch.where(expert_mask[expert_idx])         # 哪些 token 选了这个专家
    current_state = hidden_states[None, top_x].reshape(-1, hidden_dim)
    current_hidden_states = self.experts[expert_idx](current_state) * routing_weights[top_x, idx, None]
    final_hidden_states.index_add_(0, top_x, current_hidden_states)
```

三件事值得盯住：

1. **路由器只是一个线性层加 softmax 加 top-k。**没有任何"专家擅长什么"的先验，专家分工是训练时自己长出来的。研究者事后看，专家往往按 token 类型（标点、数字、代码关键字）分，而不是按人类想象的"数学专家""法律专家"分。
2. **每个专家只处理选了它的 token。**`torch.where` 挑出下标，专家只对这一小撮 token 做前向。这就是省算力的来源。
3. **选中的权重重新归一后加权求和。**一个 token 的输出是 k 个专家输出的加权和，路由器的分数就是权重。

### 负载均衡：不能让一个专家累死

放任不管，路由器会迅速学会把所有 token 都送给最初表现稍好的一两个专家（富者愈富），其余专家没有梯度、永远学不会。所以 MoE 训练几乎都带一个**辅助损失**：

```python
# load_balancing_loss_func 的核心
tokens_per_expert = expert_mask.float().mean(dim=0)     # 每个专家实际接到的 token 比例
router_prob_per_expert = routing_weights.mean(dim=0)    # 路由器给每个专家的平均概率
loss = torch.sum(tokens_per_expert * router_prob_per_expert) * num_experts
```

两个向量都均匀时这个乘积最小。它把"分配要均匀"这件事直接写进了损失函数。DeepSeek-V3 换了一种做法：不加损失，给每个专家一个可调偏置，谁接的少就把谁的偏置调高，避免辅助损失干扰主目标。方法在变，要解决的问题没变。

### MoE 省的是算，不是搬

第 5 篇的结论在这里要再用一次。MoE 每个 token 只算 k 个专家，但**所有专家的参数都得在显存里**，因为下一个 token 可能选任何一个。Mixtral 8x7B 推理时要装 467 亿参数，占的显存和一个 470 亿 dense 模型一样，只是算得像 130 亿。

所以 MoE 的账是：**训练算力和推理算力大幅下降，显存占用不降，通信开销上升**（专家分布在多张卡上时，token 要在卡之间来回搬，叫专家并行）。它适合有大量显存、追求吞吐的服务方；对单卡跑本地模型的人，同样显存下 dense 往往更划算。

## 六、推理时的规模：第四个量

前三节的 scaling 都发生在训练时。第 4 篇讲过的推理模型引入了第四个可以砸算力的地方：**回答时多想一会儿**。同一个模型，生成更长的思考、或者采样多次取多数，在数学和代码任务上的准确率随推理算力按类似的幂律上升。

这改变了"大"的定义。一个小模型多想十倍时间，在某些任务上能追上一个大模型一步到位。训练算力、推理算力，两者可以互相换。

## 七、回到那个问题

模型越大越聪明吗？拆开看：

- **同一代、同样训法，大的更强。**scaling law 没失效。
- **同样预算，过大是浪费。**Chinchilla：参数和数据要按比例。
- **算上推理成本，小而多训更划算。**Llama 3 8B 吃了百倍数据。
- **蒸馏让小模型继承大模型的一部分。**
- **MoE 让"总参数"和"每次算多少"脱钩。**看 MoE 模型要看激活参数。
- **推理时算力是新的杠杆。**

所以看一个模型，不要只看参数量这一个数字，至少看四个：总参数、激活参数、训练 token 数、训练数据里有多少是蒸馏来的。

## 本篇要点

- Scaling law：损失随参数、数据、算力按幂律下降，三者要一起长；它描述的是损失，不是某项能力。
- Chinchilla：固定训练预算下参数和数据应等比例增长（约 20 token / 参数）；此前的大模型普遍数据不足。
- 算上推理成本后，行业转向小模型过度训练（Llama 3 8B 约 1900 token / 参数）；数据的去重、过滤、合成成为新的主战场。
- MoE 用路由器让每个 token 只过 k 个专家：省训练和推理算力，不省显存；需要负载均衡。
- 推理时算力是第四个可以 scale 的量，训练算力和推理算力可以互换。

## 思考题

Mixtral 8x7B 的总参数是 467 亿而不是 8 × 70 = 560 亿，少的那部分是什么？（提示：MoE 只替换了 Transformer block 里的哪一部分。）负载均衡损失为什么用"实际分配比例 × 路由概率"的乘积，而不是直接惩罚分配比例的方差？（提示：哪一项是可导的。）

---

*相关代码：`src/transformers/models/mixtral/modeling_mixtral.py`（`MixtralSparseMoeBlock`、`load_balancing_loss_func`）。文中的模型参数与训练数据量取自各自的技术报告，具体数字随版本更新，比例关系是要点。*
