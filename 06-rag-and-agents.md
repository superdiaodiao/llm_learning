# 06 · 应用层：RAG 与 Agent

> 中文 · [English](en/06-rag-and-agents.md)

> 系列《从零看懂大模型》第 6 篇。前五篇讲"模型"，这一篇把模型接到真实产品上。应用层种类很多，本篇只讲两个最承重的基元——RAG 和 Agent——因为绝大多数 LLM 应用要么是它们之一，要么是它们的组合。

## 应用地图：先看全貌

![LLM 应用地图](images/m6_app_landscape.png)

按"模型还需要什么额外能力"来分，应用可以归成几个基元：只需生成（纯 prompt：翻译、摘要、分类）、需要外部知识（RAG）、需要采取行动（Agent）、需要固定格式（结构化抽取）、需要专精（微调任务模型）、需要处理图像语音（多模态）。

真实产品是这些基元的组合：客服助手 = RAG（查政策）+ Agent（下单退款）；编程 Copilot = Agent（读文件跑测试）+ RAG（查代码库）。RAG 和 Agent 是两堵"承重墙"，分别给模型补上它天生没有的两样东西：**外部知识**和**采取行动的能力**。懂了这两个基元，就能把任何复杂产品拆解成"基元 + 胶水"。

## RAG：给模型外挂一个可更新的知识库

![RAG 完整流水线](images/m6_rag_pipeline.png)

模型的权重是训练时冻结的，不知道你公司的内部文档，也不知道昨天的新闻。RAG（检索增强生成）不改模型，只在提问时把相关资料"喂"进提示词，让模型基于资料回答——可溯源、易更新。

流水线分两段：

- **离线索引**（建库，做一次）：文档 → 切分成块 → 每块经 embedding 变成向量 → 存入向量库。
- **在线问答**（每次提问）：问题 → 用同一个 embedding 变成向量 → 在向量库里找"夹角最小"的几块 → 可选重排 → 拼进提示词 → LLM 生成。

核心就是第 1 篇那个"语义相近 = 向量相近"：检索等于在向量空间里找离问题最近的知识块。

### 两个常见误解

![检索器是可插拔的](images/m6_retriever_pluggable.png)

**RAG 不等于知识库。** 知识库是一个数据源；RAG 是"提问时取回相关资料、塞进提示"这个做法。"知识库问答"只是 RAG 最常见的一个实例。RAG 检索的对象可以是数据库、网页、API、聊天记录。

**不用向量库也算 RAG。** RAG 的 R 是 Retrieval，任何"取回相关外部信息"的方式都算：关键词全文检索（BM25）、SQL 查库、网页搜索、API 调用，甚至上下文放得下时直接把整份文档塞进去。向量库只是做**语义检索**最流行的实现。按场景选：模糊的、语义的、非结构化文本用向量；精确的词、ID、结构化数据用关键词或 SQL；两者都要就混合检索。

一个更本质的模型：模型的知识分**参数化**（烤进权重，冻结、不可溯源）和**非参数化**（运行时取回、可更新、可溯源）。RAG 就是用后者补前者的不足；怎么取是实现细节。

### 重排（reranker）：第二道更精细的筛子

![两阶段检索](images/m6_reranker.png)

向量检索用的是**双编码器**：query 和文档各自编码成向量再比夹角。快——文档向量可以提前算好，查询时对百万文档做近邻搜索也是毫秒级；但粗——query 和文档从没在同一个模型里"见过面"。

**交叉编码器**把 query 和某个文档拼在一起送进模型，让它俩在同一次 attention 里互相看，最后吐一个相关度分数。准得多，但没法预先算，每个 (query, 文档) 对都得现场跑。

谁都不能单干，所以用漏斗：先用快的把百万粗筛到上百（负责"别漏"），再用准的在上百里精排出最相关的三五个（负责"排得对"）。这正是第 1、2 篇两个基元的一次漂亮回收。

## Agent：给模型套上"思考-行动-观察"的循环

![Agent 循环](images/m6_agent_loop.png)

模型本身只会预测文字，它没法查天气、跑代码、访问数据库。Agent 是套在模型外面的一个循环，把"预测文字"变成"能影响世界的行动"：

1. 把可用工具清单和用户问题一起塞给模型；
2. 模型输出一个**结构化的工具调用**（"我要调 weather_api，参数 city=北京"）；
3. **运行时**（不是模型）真的去执行，拿到结果；
4. 把结果追加回去，再问模型一次；
5. 循环，直到模型说"信息够了"，输出最终回答。

关键洞察：**"思考"和"决定调什么工具"都只是模型在生成文字**——是外面这个循环，把文字生成变成了会用工具、能分步办事的 agent。模型负责决策，运行时负责执行，结果再喂回去。

当"要不要检索、检索什么"也交给模型自己决定时，RAG 就和 Agent 融合了——检索变成 agent 能调的一个工具，业界叫 **agentic RAG**。

## 本篇要点

- 应用层的两个承重基元：RAG 补外部知识，Agent 补行动能力；真实产品是它们的组合。
- RAG 是"检索后生成"的做法，不等于知识库，也不限于向量库；本质是用非参数化知识补参数化知识。
- 重排用交叉编码器精排粗筛结果，两阶段漏斗兼顾快和准。
- Agent = 模型 + 循环 + 工具 + 执行运行时；模型永远只是生成文字。

## 思考题

为什么向量检索（双编码器）不能直接换成更准的交叉编码器一步到位？（提示：想想"能不能预先算好"。）

---

*下一篇进入更深的两个方向：AI 记忆系统（第 7 篇）和 Agent 架构（第 8 篇）。*
