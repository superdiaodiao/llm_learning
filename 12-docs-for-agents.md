# 12 · 写给 Agent 看的文档：规则文件与 Skill 写法

> 中文 · [English](en/12-docs-for-agents.md)

> 系列《从零看懂大模型》第 12 篇。第 11 篇把 skills、hooks、MCP 放回了 Agent = 模型 + Harness 的框架；本篇只讲一件事：**规则文件和 skill 到底怎么写**。原理只有一条——这些文件的读者是模型，不是人——其余全是这条的推论。

## 先立一条：读者是模型

给人写文档，默认读者有记性、有常识、会追问。给模型写，这三样都没有。前几篇讲过的机制，直接决定了写法：

| 模型的性质 | 出处 | 对写法的要求 |
|---|---|---|
| 每一轮从头读一遍整个上下文，读完就忘 | 03、05 | 每一行每轮都花钱；只写每轮都需要的 |
| 窗口有限，塞得越多每条分到的注意力越少 | 03、11 | 短；能不进窗口的放外面按需取 |
| 开头和结尾记得牢，中间容易丢 | 11 | 最重要的规则放最前面或最后面 |
| 写在提示里的禁令它会忘、会被绕过 | 11 | 必须保证的事交给代码（hook），不靠它自觉 |

![读者是模型：四条性质，四条写法](images/ad_reader_is_model.png)

记住这张表，下面所有"应该怎么写"都不用背。

## 一、项目规则文件

### 它是什么、什么时候被读

`CLAUDE.md`、`AGENTS.md`、`.cursorrules`、`.github/copilot-instructions.md` 是同一样东西在不同工具里的名字：一份放在仓库里的说明，harness 在**每一轮**开始时把它读进上下文，位置通常紧跟系统提示。它不是给一次任务写的，是给这个仓库里**所有**任务写的。

这决定了它的两个特点：每一行都在每一轮里占窗口；它出现在上下文的最前面，是模型记得最牢的位置之一。前者要求它短，后者要求它只放最重要的事。

### 写什么

只写四类，都是"模型自己发现不了、又每次都需要"的信息：

1. **怎么跑。** 装依赖、跑测试、起服务、构建的确切命令。模型最常犯的错是猜命令，一行就省掉一轮试错。
2. **目录地图。** 代码在哪、测试在哪、配置在哪、生成文件在哪（别手改）。五到十行。
3. **约定。** 和通用做法**不一样**的地方：命名、提交信息格式、用哪个包管理器、不用哪个库。和通用做法一样的不用写。
4. **禁区。** 不能碰的文件、不能做的操作、需要人确认的动作。

### 不写什么

- **模型本来就会的。** "Python 用四空格缩进""写清晰的变量名"，这些它在预训练里学过几十亿遍，写进去只占地方。
- **一写就过时的。** 版本号、当前分支、正在做的功能。过时的规则比没有规则更糟，它会照着错的做。
- **背景故事。** 项目的历史、为什么当年选了这个框架。模型不需要理解，只需要照做。
- **一次性的要求。** 那是这一轮对话该说的话，不是规则。

### 分层放

规则天然分三层，放三个地方，各管各的：

| 层 | 位置 | 放什么 | 谁维护 |
|---|---|---|---|
| 个人 | 用户目录（如 `~/.claude/CLAUDE.md`） | 你自己的偏好：回复语言、风格 | 你 |
| 项目 | 仓库根目录 | 团队约定：怎么跑、目录、禁区 | 团队，进版本管理 |
| 局部 | 子目录 | 只对这个目录有效的：特殊构建、特殊格式 | 负责这块的人 |

![规则分三层](images/ad_rules_layers.png)

个人偏好混进项目文件，是最常见的错误：团队里每个人都会读到"请用中文回复"。反过来，团队约定写在个人文件里，别人就不知道。

### 控制长度

一份规则文件会自然越写越长：每次出问题就加一条。加的时候没人删，半年后它有两百行，模型对每一行的注意力都被稀释了，真正重要的那几条反而不灵。

两个办法：一是**定期删**，每加一条问一句"这条是每一轮都需要的吗"；二是**渐进披露**，把长的细节移到单独文件，规则里只留一行"部署流程见 `docs/deploy.md`"，用到时模型自己会去读。

### 一份够用的例子

```markdown
# 项目规则

## 运行
- 安装：`uv sync`；测试：`uv run pytest -q`；起服务：`uv run uvicorn app.main:app`
- 提交前跑 `uv run ruff check .`，红了别提交

## 结构
- `app/` 业务代码，`app/api/` 路由，`app/core/` 领域逻辑
- `tests/` 与 `app/` 同构；`migrations/` 由 alembic 生成，别手改

## 约定
- 包管理只用 uv，不用 pip / poetry
- 提交信息用 `type(scope): 一句话`，英文
- 新的外部调用一律走 `app/core/clients/`，不在路由里直接发请求

## 禁区
- 不改 `migrations/` 里已有文件；不删 `tests/` 里的用例
- 涉及 `.env`、密钥、生产配置的改动先问人
```

二十来行，四个板块。每一行都是"模型猜不到、又每次都用得上"的。

## 二、Skill

### 结构

一个 skill 是一个文件夹：

```
pdf-report/
├── SKILL.md          # 必须：元数据 + 怎么做
├── scripts/          # 可选：确定性的活交给脚本
│   └── render.py
├── templates/        # 可选：模板
│   └── report.md
└── references/       # 可选：细节文档，按需读
    └── style-guide.md
```

`SKILL.md` 开头是一段元数据，最少两项：`name` 和 `description`。正文是步骤。

### description 决定会不会被用

第 11 篇讲过渐进披露：平时模型只看到所有 skill 的 description 列表，任务命中才加载全文。所以 description 不是"这个 skill 是什么"的简介，是**触发条件**：什么任务、什么关键词、什么文件类型出现时该用它。

```yaml
# 差：像产品介绍
description: 一个强大的 PDF 报告生成工具

# 好：写触发条件
description: 当用户要把数据、表格或分析结果做成 PDF 报告时使用；
  提到"报告""PDF""导出""汇报材料"都算。不用于读取或解析已有 PDF。
```

"不用于……"那半句同样重要：它防止 skill 被错误触发。description 是整个 skill 里最值得反复改的一段，因为它决定了其余部分有没有机会被读到。

### 正文决定用得对不对

正文写给已经决定用这个 skill 的模型看，所以只写**怎么做**：

- **步骤**，按顺序，每步一个动作。
- **判断标准**：什么情况走哪条分支；什么算完成。
- **输出格式**：产物长什么样，放哪。
- **检查清单**：交付前核对什么。

不写的：这个领域的背景知识（模型会）、为什么这么设计（模型不需要）、礼貌用语。

### 能确定的事，别让它猜

模型擅长判断，不擅长精确执行。凡是"每次都一样、有唯一正确做法"的步骤——渲染、格式转换、命名、校验——写成脚本放进 `scripts/`，正文里写"运行 `scripts/render.py`"。这和第 11 篇 hook 的逻辑是一回事：**该由代码保证的，不交给模型自觉。**

同理，长的参考材料放 `references/`，正文里写一行"配色和字体规范见 `references/style-guide.md`"。模型需要时会去读，不需要时不占窗口。

![一个 skill 的结构](images/ad_skill_anatomy.png)

### 一份够用的例子

```markdown
---
name: pdf-report
description: 当用户要把数据、表格或分析结果做成 PDF 报告时使用；提到"报告""PDF""导出""汇报材料"都算。不用于读取已有 PDF。
---

## 步骤
1. 确认三件事：报告给谁看、要几页、有没有指定模板。缺一项就问，不要猜。
2. 用 `templates/report.md` 起草，章节顺序：结论 → 数据 → 方法 → 附录。
3. 数字一律来自用户给的文件，不补、不估。找不到的写"缺数据"。
4. 运行 `python scripts/render.py draft.md out.pdf` 生成 PDF。不要手写 PDF。

## 完成标准
- 第一页能在 30 秒内看懂结论
- 每个图表有标题和数据来源
- 文件名：`{主题}-{日期}.pdf`

## 交付前核对
- [ ] 数字和源文件一致
- [ ] 没有"待补充"占位
- [ ] 配色符合 `references/style-guide.md`
```

## 三、怎么知道写好了

规则文件和 skill 都是提示词，改了不测就是在赌（第 11 篇）。两个便宜的测试：

**触发测试**（只针对 skill）：准备五个该触发的任务、五个不该触发的，看 description 有没有把它们分开。错触发比漏触发更常见，因为人写 description 时总想写得宽一点。

**有无对比**：同一批真实任务，带着规则或 skill 跑一遍，不带跑一遍，比结果。差别不明显，说明这份文件在占窗口却没干活，删。

### 常见坑

| 坑 | 症状 | 修法 |
|---|---|---|
| 越写越长 | 规则两百行，最重要的几条不灵了 | 定期删；细节移到单独文件按需读 |
| description 写成简介 | skill 该用时没被用，不该用时被用 | 改成触发条件，加"不用于" |
| 当提示词库堆 | 二十个 skill 互相重叠 | 一个 skill 一个任务；重叠的合并 |
| 写模型已会的 | 规则里全是通用编程常识 | 只留和通用做法不一样的 |
| 个人偏好进团队文件 | 同事被要求用你的语言回复 | 分层放 |
| 靠禁令保证安全 | "不要删文件"写了，文件还是被删了 | 写成 hook |
| 一写就过时 | 规则里的分支名、版本号早变了 | 不写会变的东西 |

## 本篇要点

- 读者是模型：每轮重读、读完就忘、窗口有限、位置有偏、禁令会被绕过。所有写法都从这五条推出来。
- 规则文件只写四类：怎么跑、目录地图、和通用做法不同的约定、禁区。分个人 / 项目 / 局部三层放。定期删。
- Skill 的 description 是触发条件，不是简介；正文只写步骤、判断标准、输出格式、检查清单。
- 确定性的事交给脚本，长材料放参考文件按需读。
- 改了要测：触发测试 + 有无对比。

## 思考题

为什么 skill 的 description 比正文更值得反复修改？"请不要删除任何文件"这句话，写在规则文件里和写成一个 hook，在什么情况下会表现出差别？（提示：第 11 篇"该用代码保证的事"。）

---

*文件名、目录位置、元数据字段在不同工具和版本里会变，本篇只以当前常见形式举例。不变的是那张表：读者是模型。*
